#pragma once

#include <vector>

namespace verilog {

  struct Generator {
    //  Generates a random input of a given size
    void generate_inputs(std::vector<bool> & inputs, int size) {
      // TODO
    }
  };

}
