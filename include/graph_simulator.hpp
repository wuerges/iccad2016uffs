#pragma once

#include <graph_model.hpp>
#include <vector>

namespace verilog {
  namespace graph {
    void simulate(std::vector<bool> & inputs, std::vector<bool> & outputs, G & graph) {
      // TODO
    }
  }
}
